const NotFoundPage = () => (
    <main className="page-shell">
        <section className="page-document" aria-labelledby="not-found-title">
            <p>Not found</p>
            <h1 id="not-found-title">This page is not on the menu</h1>
            <p>The page may have moved, or it may not exist yet.</p>
        </section>
    </main>
);

export default NotFoundPage;
